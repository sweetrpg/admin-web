<nav class="nav">
	<div class="nav-brand">
		<a href="#(meta.rootURL)">
			<img src="#(meta.sharedAssetsURL)/static/sweetrpg-logo-black.svg" alt="SweetRPG" class="nav-logo nav-logo-light">
			<img src="#(meta.sharedAssetsURL)/static/sweetrpg-logo-white.svg" alt="SweetRPG" class="nav-logo nav-logo-dark">
		</a>
		<a href="#(meta.basePath)/">SweetRPG Admin</a>
	</div>
	<a href="#(meta.basePath)/">Banners</a>
	<a href="#(meta.basePath)/banners/new">New Banner</a>
	<a href="#(meta.basePath)/maintenance-modes">Maintenance</a>
	<a href="#(meta.basePath)/users">Users</a>
	<div class="nav-spacer"></div>
	#if(user):
		<span class="nav-user">#(user.name)</span>
		<form action="#(meta.logoutURL)" method="post"><button type="submit" class="btn btn-ghost">Log Out</button></form>
	#else:
		<a class="btn btn-ghost" href="#(meta.loginURL)">Log In</a>
	#endif
</nav>
